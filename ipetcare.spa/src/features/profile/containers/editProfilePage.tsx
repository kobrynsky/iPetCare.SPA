import React from "react";

export const EditProfilePage = () => (
    <div>
        user.id
        user.imageUrl
        user.firstName
        user.lastName
        user.email
        user.placeOfResidence
        user.role
        user.specialization
        user.userName
    </div>
)